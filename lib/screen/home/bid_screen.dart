import 'package:flutter/material.dart';

class BidScreen extends StatelessWidget {
  static const routeName = 'bid_screen';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Bid List'),
      ),
    );
  }
}
